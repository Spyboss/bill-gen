# Server settings
PORT=8080
NODE_ENV=production

# MongoDB settings - Update with actual MongoDB Atlas URI in Railway
MONGODB_URI=mongodb+srv://${MONGO_USER}:${MONGO_PASSWORD}@${MONGO_CLUSTER}.mongodb.net/tmr-bill-generator?retryWrites=true&w=majority

# CORS settings for frontend hosted on Cloudflare Pages
CORS_ORIGIN=https://tmr-bill-generator.pages.dev

# Logging
LOG_LEVEL=info 